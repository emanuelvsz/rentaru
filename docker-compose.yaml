version: '3'

services:
  api:
    image: python:3.10
    command: bash -c "pip install -r requirements.txt && python manage.py migrate && python manage.py runserver 0.0.0.0:8001"
    volumes:
      - ./seu_projeto:/app
    ports:
      - "8001:8001"